default = "dev"

[crates.dev]
  image = "qurx/withmock-dev:1.19"
  mount-home = true
  project-mount = "/project"
  working-dir = "project,match"
  cap-add = ["SYS_PTRACE"]
  shell = "/bin/sh"
  network = "host"
  export-bin = [
    "/usr/local/go/bin/*",
    "/go/bin/*",
    "/usr/bin/np*",
  ]

